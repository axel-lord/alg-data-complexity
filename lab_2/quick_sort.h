#include "sized_array.h"

/* sorts given sized_array in-place
 * Parameters:
 * Arr, array to sort
 * Returns:
 * 1, if everything went as planned
 * 0 otherwise
 */
int quicksort_array(sized_array * Arr);
